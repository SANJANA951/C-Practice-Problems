#include <stdio.h>

int main() {
    int r = 5;
    int height = 10;

    printf("The area of the circle with radius %d is %.2f\n",
           r, 3.14 * r * r);

    return 0;
}

/*int main() {
    int r = 5;
    int height = 10;

    printf("The volume of the cylendar with the circle with radius %d is %.2f\n",
           height, r, 3.14 * r * r * height);

           */
